type ProductionSite {
  id: ID!
  name: String!
  companyId: ID!
  cityId: ID!
  createdAt: String!
  updatedAt: String!
}

type ProductionSiteSelectOption {
  id: ID!
  name: String!
  companyId: ID!
  cityId: ID!
  createdAt: String!
  updatedAt: String!
}

type ProductionSiteRelation {
  id: ID!
  name: String!
  city: City!
  company: Company!
}

type Query {
  productionSites: [ProductionSiteSelectOption!]!
  productionSite(id: ID!): ProductionSiteSelectOption!
  getProductionSitesForSelect: [ProductionSite!]!
}

input CreateProductionSiteInput {
  name: String!
  companyId: ID!
  cityId: ID!
}

input UpdateProductionSiteInput {
  name: String!
  companyId: ID!
  cityId: ID!
}

type Mutation {
  createProductionSite(input: CreateProductionSiteInput!): ProductionSite!
  updateProductionSite(input: UpdateProductionSiteInput!): ProductionSite!
  deleteProductionSite(id: ID!): Boolean!
}
